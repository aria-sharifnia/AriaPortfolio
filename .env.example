# Copy to .env.local and provide your own values if you have access.
# This repository intentionally omits real values so it cannot be run publicly.

# Required (no default provided)
VITE_STRAPI_URL=
